name: terraform-workflow-test

on:
  workflow_dispatch:
 

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Create Terraform API token
        run: |
            WORKSPACE_ID=($(curl \
            --header "Authorization: Bearer UxSwoTHSAY3ELA.atlasv1.HNCtRjNpZBpzhQy6wRqr2QzzlUlimZe53wHT4fjmugGbBfC6YHBTNY0eDjtEdwznySc" \
            --header "Content-Type: application/vnd.api+json" \
            https://app.terraform.io/api/v2/organizations/MyFirstOrg-BalajiM/workspaces/MyFirstWorkspace\
            | jq -r '.data.id'))
            echo "token: $WORKSPACE_ID"
      # - name: 'terraform api'
      #   run: |
      #     ls -a
      #     echo "testing the workflow "
      #     chmod +x ./.github/workflows/scripts/terraform-api.sh
      #     ./.github/workflows/scripts/terraform-api.sh
